# 事前登録（Pre-registration）: LLMの二段階レビューは名寄せを直せるか

- 作成日: 2026-07-10（**A/B/C走行の前に**確定・公開pushしてから走行する）
- 被験モデル: **gpt-5.4-mini-2026-03-17**、`reasoning_effort=low`（設定で結果が動くためpin）。走行日を記録。
- スコープ: Entity Matching（候補ペア所与の同一人物判定）。blockingは対象外。

## データと正解（gold）
- 対象: 日本人サッカー選手（Wikidata日本語ラベル ∩ Transfermarkt/dcaribou）。ペア160件＝確認120（真マッチ60・非マッチ60）＋難例40（hard-pos 20・hard-neg 20）。80/80バランス。
- 表層形: JP側=Wikidata ja label＋P54クラブ、ローマ字側=dcaribou name＋transfersクラブ（異ソース部分一致）。
- **モデル入力から除外**: 生年月日・Wikidata QID・Transfermarkt ID・URL・外部識別子（リーク防止・dev-20でleak=0検証済）。
- gold: 同一player_id/QIDによるby-construction＋**独立属性の生年月日（Wikidata P569 × dcaribou）で機械検証（157/160確認・矛盾0）**、要人手3件（P030/P143/P149）は氏名で裁定。gold誤り率は報告する。

## 実験条件
- **A（初回）**: prompt_Aで初回判定。
- **B（独立再実行）**: Aと**バイト単位で同一**のprompt_A、fresh context。
- **C（二段階レビュー）**: prompt_A ＋ Aの完全な出力を叩き台として提示し、中立指示でKEEP/CHANGE。
- 測る介入=「初回出力を叩き台として同じモデルに渡すワークフロー」。構成要素は分離しない。evidence_codesは因果的根拠として扱わない。

## 主要評価項目（1本・確認セット120のみ）
- **Δ = balanced accuracy_C − balanced accuracy_B**（balanced accuracy=(sensitivity+specificity)/2）。
- **CI**: 正例上のΔ_sens(n=60)と負例上のΔ_spec(n=60)を各々Newcombe対応差法で区間推定し0.5:0.5を**MOVER-Dで合成**、または**選手単位の対応ブートストラップ**。120対プールの単一Newcombeは用いない（overall accuracyの区間になるため）。
- **判定（3分類）**: CI全体が0超→改善／0未満→悪化／またぐ→この標本では識別不能。±5ptは図の実務参照帯。**formal equivalence(TOST)は主張しない**。
- precisionは有病率依存ゆえ実運用値として単独主張しない。

## 副次指標（2-3本）
- **正解破壊**: A→C遷移（正→正/誤→正/正→誤/誤→誤）、純改善=(誤→正)−(正→誤)。**破壊率の分母は層別（確認セットでAが正解だった件数・クラス別）**。生件数＋Wilson。著名度層別で併記。
- **誤り非対称**: 誤統合（別人をSAME）vs 見逃し（同一人物をDIFFERENT）の生カウント＋Wilson。
- （不一致ルーティング: 事前固定ルールとの不一致→レビュー方針。ルール未実装のため実装後に追加。ルール単体P/Rも報告。）

## 探索的（"探索的"明示・n/CI必須・見出しに昇格させない）
- 難例40の救済タクソノミー（**率も検定・効果量も記述専用＝母集団へ一般化しない**）。
- 誤り原因の3分類（読み知識欠落／翻字揺れ誤判定／別人見落とし）でnullを分解。
- 反復（B/独立3回目）による判定の揺れ＝**Z3ファイアウォール**（相互参照のみ・主役化しない）。
- 較正（高確信で誤った件数・粗2ビン）。ECE見出しは作らない。

## 予測（結果を見る前に確定・両方向とも情報量あり）
- 文献（Huang et al. 2024・Kamoi et al. 2024: 外部検証器なしの内在的自己修正は推論タスクで概ね効かない/劣化）から、**C対Bは±5pt以内（自己監査は独立再実行を明確に上回らない）と予想**。ただし名寄せは判別的事実判定で失敗機構が異なるため保証はしない。
- **正解破壊（正→誤）は少数だが実在すると予想**。
- 初回精度は正例で高く、hard-neg（同姓・同世代）で低下すると予想。
- どの結果（改善/無効/悪化/識別不能）でも同じ報告規律を適用し、nullを後付けで美化しない。

## 開示規律
- 走らせた分析は全て開示（有意なものだけ選ばない）。p値でなく件数・効果量・CIで報告。探索を確証として書かない。主要1本のみ確認的。
- 本ファイルを**A/B/C走行の前に公開リポジトリへpush**（ローカルhashでなくサーバ側タイムスタンプで"走行前"を担保）。走行後はprompts/run_resultsのハッシュを追記。
